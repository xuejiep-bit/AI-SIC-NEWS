# 临时探针：从云端实测 Yahoo Finance 接口。用完即删。
name: Probe Yahoo

on:
  push:
    branches: [claude/dreamy-cray-GFpRu]
    paths: [.github/workflows/probe-yahoo.yml, scripts/probe_yahoo.py]

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python3 scripts/probe_yahoo.py
